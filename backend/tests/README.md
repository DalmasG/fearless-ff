# Backend tests — placeholder

Add integration tests with each implemented module. Cover access boundaries, invalid input, state transitions, duplicate callbacks, and failures from external providers. Use test credentials and deterministic fixtures; never real payments or production email recipients.
