# HTTP adapters — placeholder

Future location for request validation, route registration, response serialization, and error handling. No endpoint is currently registered here.

Expose only confirmed public programme data. Validate write requests server-side, enforce ownership/roles, and avoid including private submission or order fields in public responses. Document endpoints before wiring frontend requests.
