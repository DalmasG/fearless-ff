# Festival information — placeholder

Status: planned, not implemented.

## Responsibility

About/story content, guidelines, FAQs, deadlines, contact information and social links.

## Proposed endpoints

GET /api/v1/festival; GET /api/v1/festival/guidelines

## First implementation step

Use organizer-approved content and validated external URLs. No contact form is implied by a footer link.

Add service logic, validated input schemas and repository interfaces here when this feature is commissioned. Keep HTTP adapters in `backend/src/http/` and provider-specific code in `backend/src/integrations/`.
