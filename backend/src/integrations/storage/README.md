# Storage adapter — placeholder

Future film artwork and submission attachment storage. Keep private submission assets separate from public festival imagery. Define size/type limits and access controls before enabling uploads. Current public assets remain in `public/assets/`.
