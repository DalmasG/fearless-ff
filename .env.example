# The frontend currently uses local preview data and needs no credentials.
# Add required names here when an integration is implemented.
# Keep secrets in ignored environment files or the deployment secret store.
